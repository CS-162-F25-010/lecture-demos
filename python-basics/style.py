#from package import item_a,\
#    item_b,\
#    item_c

def main() -> None:
    # One of the rules in our style / design guide is to keep your lines of
    # code shorter than 80 characters
    my_str = ('some really really '
        'long string')

    foo(
        arg1,
        arg2,
        arg3,
        arg4
    )


if __name__ == '__main__':
    main()
